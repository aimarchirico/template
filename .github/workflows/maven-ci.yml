name: Maven - CI

on:
  push:
    branches: [main, dev]
    paths: [maven/**]
  pull_request:
    branches: [main, dev]
    paths: [maven/**]

jobs:
  ci:
    uses: aimarchirico/core/.github/workflows/reusable-backend-ci.yml@dev
