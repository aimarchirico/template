name: Docs - CI

on:
  push:
    branches: [main, dev]
    paths:
      - "**/*.md"
      - ".github/workflows/docs-ci.yml"
      - "Taskfile.yml"
      - "package.json"
      - "package-lock.json"
  pull_request:
    branches: [main, dev]
    paths:
      - "**/*.md"
      - ".github/workflows/docs-ci.yml"
      - "Taskfile.yml"
      - "package.json"
      - "package-lock.json"

jobs:
  ci:
    uses: aimarchirico/core/.github/workflows/reusable-docs-ci.yml@dev
