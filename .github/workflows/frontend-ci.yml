name: Frontend - CI

on:
  push:
    branches: [main, dev]
    paths: [frontend/**]
  pull_request:
    branches: [main, dev]
    paths: [frontend/**]

jobs:
  ci:
    uses: aimarchirico/core/.github/workflows/reusable-frontend-ci.yml@dev
    secrets: inherit

