name: Close Sub-Issues

on:
  issues:
    types: [closed]

permissions:
  issues: write

jobs:
  cascade:
    name: Close Sub-Issues
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: aimarchirico/commons/.github/actions/close-sub-issues@main
