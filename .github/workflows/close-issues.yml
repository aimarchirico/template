name: Cascade Close Sub-Issues

on:
  issues:
    types: [closed]

permissions:
  issues: write

jobs:
  cascade:
    name: Close Sub-Issues
    runs-on: ubuntu-latest
    steps:
      - name: Cascade Close Sub-Issues
        uses: aimarchirico/commons/.github/actions/close-issues@main
