# API
# Native dev calls the backend directly (no CORS). Web dev calls it
# cross-origin, so the backend must allow this origin (CLIENT_CORS_ALLOWED_ORIGINS).
# Path segment must match the backend context-path (server.servlet.context-path).
API_URL="http://localhost:8080/template"

# Dev
IS_DEV=true