version: '3'

tasks:
  build:
    cmds:
      - echo "No build step for skill"
  check:
    cmds:
      - echo "No check step for skill"
  fix:
    cmds:
      - echo "No fix step for skill"
  run:
    cmds:
      - echo "No run step for skill"
