# Database
DB_HOST=localhost:5432
DB_USER=user
DB_PASSWORD=password

# Reverse proxy (set by the proxy in prod, blank locally)
PROXY_SECRET=""

# Local development only (not needed in prod)
CORS_ALLOWED_ORIGINS="http://localhost:8081"
