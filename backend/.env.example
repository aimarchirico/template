# Database
DB_HOST=localhost:5432
DB_USER=postgres
DB_PASSWORD=postgres

# CORS origins for local web dev (comma-separated)
CLIENT_CORS_ALLOWED_ORIGINS=http://localhost:8081

# Set by the proxy in prod; leave blank locally
PROXY_SECRET=""
