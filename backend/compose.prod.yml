services:
  template-api:
    container_name: template-api
    image: ghcr.io/aimarchirico/template-api:latest

    ports:
      - "8080:8080"
    env_file: .env
    depends_on:
      - template-db
    restart: always
