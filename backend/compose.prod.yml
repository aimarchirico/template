services:
  template-backend:
    container_name: template-backend
    image: ghcr.io/aimarchirico/template:latest

    ports:
      - "8080:8080"
    env_file: .env
    depends_on:
      - template-db
    restart: always
