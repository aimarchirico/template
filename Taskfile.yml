version: '3'

dotenv: ['.env']

includes:
  init:
    taskfile: ./init/Taskfile.yml
    dir: ./init
  github:
    taskfile: ./.github/Taskfile.yml
    dir: ./.github
  backend:
    taskfile: ./backend/Taskfile.yml
    dir: ./backend
  frontend:
    taskfile: ./frontend/Taskfile.yml
    dir: ./frontend
  tools:
    taskfile: ./tools/Taskfile.yml
    dir: ./tools
    flatten: true

tasks:
  default:
    cmds:
      - task --list
